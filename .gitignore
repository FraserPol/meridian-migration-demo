SETUP_INSTRUCTIONS.md
node_modules
.next
.env
.env*.local
*.log
.DS_Store
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# Terraform — state and real tfvars contain plaintext secrets (RDS master
# password, Vault bootstrap token, auth_secret) once you actually run
# `terraform apply`. Only terraform.tfvars.example should ever be tracked.
infra/terraform/**/.terraform/
infra/terraform/**/*.tfstate
infra/terraform/**/*.tfstate.*
infra/terraform/**/*.tfplan
infra/terraform/**/terraform.tfvars
.env*
/.swc
